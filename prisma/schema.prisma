datasource db {
  provider = "sqlite"
  url      = "file:./quotes.db" //env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Quote {
  id     Int    @id @default(autoincrement())
  quote  String
  author String?
}
